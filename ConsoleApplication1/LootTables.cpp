#include <iostream>
#include <vector>
#include <random>

struct LootItem {
    std::string m_szItem;
    int m_iRarity;

    LootItem(const std::string& name, const int iRarity )
        : m_szItem(name), m_iRarity(iRarity) {}
};

{
    std::vector<LootItem> = {
        
    }

}
